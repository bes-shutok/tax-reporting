# Plan: Review-flag re-evaluation at the aggregation boundary

Plan review: `docs/history/reviews/2026-07-17-plan-review-review-flag-aggregation-boundary-r12.md` (r12, READY - 1 Medium doc-completeness gap folded: missing r11 fold-log entry appended; 6 simplification Lows deferred as same family as r10 L11-L14; ready=Yes) · r11 (`docs/history/reviews/2026-07-17-plan-review-review-flag-aggregation-boundary-r11.md`, NOT ready - 6 Medium fold-completeness gaps folded) · r10 (`docs/history/reviews/2026-07-17-plan-review-review-flag-aggregation-boundary-r10.md`, NOT ready - 2 High + 2 Medium fold-completeness gaps folded) · r9 (`docs/history/reviews/2026-07-16-plan-review-review-flag-aggregation-boundary-r9.md`, NOT ready - 1 High design-defect folded by removing Task 2 wholesale; 5 Medium + 17 Low findings against old Tasks 1 and 3 (now Tasks 1 and 2) folded or deferred) · r8 (NOT ready - 5 Medium + 4 Low folded; 3 architecture Mediums deferred to Monitor) · r7 (NOT ready - 1 High + 8 Medium + 8 Low folded; 2 architecture Mediums deferred to Monitor) · r6 (NOT ready - 2 Medium + 6 Low folded) · r5 (NOT ready - 1 Medium + 12 Low folded) · r4 (NOT ready - 1 High + 3 Medium + 9 Low folded) · r3 (NOT ready - 11 Medium + 8 Low folded) · r2 (ready=yes) · r1 (ready=no) · r1-pre-revision (ready=no)

## Terms

- **Aggregated review flag** - the `review_required` / `review_reason` shown on a user-visible row in the Excel report. Distinct from a per-lot or per-source-row flag, which lives inside the in-memory entry list and never reaches the user directly.
- **Zero-basis review** - per-lot flag fired when a disposal lot has `cost_eur == 0`. Already in glossary; this plan refines *where* it is re-evaluated.
- **In-asset interest** - loan repayment overshoot that occurs when a DeFi variable-rate loan accrues interest in the borrowed asset, so `repaid_amount` exceeds `received_amount` by a small percentage with no cross-year anomaly.
- **Zero-basis reason prefix** - any user-facing review reason whose rendered text starts with `"Zero EUR value for known crypto asset"`, `"Zero acquisition cost"`, or `"Zero disposal proceeds"`. The aggregated-row filter strips reasons by stable prefix rather than by full-string equality, so minor wording edits to the upstream literals (e.g. rewording `"likely Koinly tracking entry or data error"`) do not silently break the strip set.
- **Sentinel + detail split (loan status)** - `LoanActivityEntry.balance_status` is a closed set of sentinel constants (`LOAN_STATUS_SETTLED`, `LOAN_STATUS_OPEN_LOAN`, `LOAN_STATUS_IN_ASSET_INTEREST`, `LOAN_STATUS_NO_EUR_PRICE`, `LOAN_STATUS_OVERPAID_VERIFY`). Any varying text (e.g. overshoot percentage) goes in a new sibling `balance_detail: str | None` field. The Excel sheet's fill-color logic matches on the sentinel, never on the rendered string.

## Gist & Examples

### Problem

Two review flags in the crypto Excel export fire on rows where the underlying signal is stale relative to the user-visible aggregated state:

1. **Crypto Gains - "Zero EUR value…" / "Zero acquisition cost…" on aggregated disposals.** `_aggregate_capital_entries` (`aggregation.py:330-331`) computes the aggregated row's `review_required` as `any(lot.review_required for lot in group)` and joins every lot's `review_reason`. A single noisy lot (e.g. one Koinly `FEE` tracking entry, or one reward lot with no price data) inside a multi-lot disposal poisons the aggregated row's flag even when the aggregated `cost_eur`, `proceeds_eur`, and `gain_loss_eur` are all non-zero and material. Several such rows appear in the 2025 export.
2. **Loan Activity - "Overpaid (cross-year loan?)" on 2 rows.** `_extract_loan_activity` (`loan_activity.py:108-112`) labels any asset with `received_amount − repaid_amount < 0` as cross-year. In the 2025 export both flagged rows (LBTC overshoot 0.36%, WBTC overshoot 0.59%) are in-asset interest, not cross-year principal. LBTC additionally has `received_value_eur == 0` for every row, so no EUR-based classification is possible at all.

(The original item 2, "Crypto Supplementary reward dust summary on popular-asset zero-value rewards", was removed at r9 and preserved as a deferred feature for a future focused plan; see `docs/history/feature-notes/2026-07-15-review-flag-deferred-findings.md` §7. The r9 panel surfaced a design-level defect: popular-token-set membership is the wrong discriminator for the dust-vs-detail split because the set is an explicit allowlist for flagging, not a Koinly-can-price list.)

### Fix principle (shared)

Re-evaluate each review signal against the user-visible aggregated state, not the per-lot / per-source-row state. The per-lot signal stays in the in-memory entry list (and the per-lot log warnings), but the flag rendered on the aggregated row is re-derived from the aggregated values.

### Examples (before → after)

**Crypto Gains, a multi-lot disposal with reward-derived lots:**

Before:
```
Cost EUR=<non-zero>  Proceeds EUR=<non-zero>  Gain=<non-zero>
Review flag = YES: Zero EUR value…; Zero acquisition cost…
```

After:
```
Cost EUR=<non-zero>  Proceeds EUR=<non-zero>  Gain=<non-zero>
Review flag = NO   (aggregated values are non-zero and material; per-lot noise dropped)
```

(Specific asset, wallet, lot count, and EUR figures omitted from this record as personal portfolio data.)

The per-lot all-zero entry is still in `context.review_entries` and logged at WARNING; only the aggregated row is cleaned up.

**Loan Activity, WBTC:**

Before: `Balance Status = "Overpaid (cross-year loan?)"`, light-red fill.
After: `Balance Status = LOAN_STATUS_IN_ASSET_INTEREST` (rendered text `"Likely in-asset interest"`), `Balance Detail = "overshoot 0.5853%"`, neutral fill (no fill).
LBTC: `Balance Status = LOAN_STATUS_NO_EUR_PRICE` (rendered text `"Cannot classify: no EUR price data"`), yellow fill.
Repayment-only asset (`received_amount == 0`): `Balance Status = LOAN_STATUS_OVERPAID_VERIFY`, light-red fill (treated as unbounded overshoot - see Invariant 4).

## Evaluation Criteria

**Quality dimensions:**

- **Correctness (signal preservation):** when *every* lot in an aggregated disposal is all-zero, the flag still fires. When only some lots are noisy, the aggregated flag is dropped iff aggregated `cost > 0 AND proceeds > 0 AND |gain| >= _MATERIALITY_THRESHOLD`. Verified by unit tests covering both branches.
- **Correctness (non-zero-basis reasons preserved):** reasons unrelated to zero-basis (phantom lot, operator-origin review, homoglyph, OGR override, missing-cost-basis-with-impact, foreign-tax parse failure) survive aggregation unchanged. Verified by a per-reason parametrized test.
- **Correctness (loan classification):** four fill behaviors across five sentinel constants - small-overshoot-with-EUR (`LOAN_STATUS_IN_ASSET_INTEREST`, neutral), no-EUR-price (`LOAN_STATUS_NO_EUR_PRICE`, yellow), large-overshoot (`LOAN_STATUS_OVERPAID_VERIFY`, light-red), and repayment-only with `received_amount == 0` (`LOAN_STATUS_OVERPAID_VERIFY`, light-red, treated as unbounded overshoot). Each emits the correct sentinel and fill. Verified by parametrized tests at the boundary (0.99%, 1.00%, 1.01%) and a dedicated `received_amount == 0` test.
- **Non-regression:** existing `_aggregate_capital_entries` tests (`tests/unit/application/test_crypto_reporting.py` line 928+, 21 cases) continue to pass without modification *except* the cases this plan intentionally changes (mixed-lot aggregated flag, all-zero-lot aggregated flag); those are updated in-task with a cited design invariant.
- **Maintainability:** no new public API surface beyond one module-level symbol (`LOAN_OVERSHOOT_INTEREST_PCT` in `loan_activity.py`, consumed by tests and `_extract_loan_activity`). The Task 1 filter is inlined as a private helper inside `aggregation.py` (single caller, single source of truth for `_MATERIALITY_THRESHOLD`); sheet-writing logic stays where it is.
- **Observability:** dropped per-lot noise still appears in `context.review_entries` and the WARNING log; the Excel row is the only thing cleaned up. No data is silently lost.

**Release gates:**

- `uv run pytest tests/unit/application/test_crypto_reporting.py tests/unit/application/persisting/test_loan_activity_sheet.py` passes.
- `uv run pytest -m unit` passes (no broader regressions).
- `uv run ruff check` passes on touched files.
- Manual re-run on the user's 2025 export confirms the four Crypto Gains rows and the LBTC/WBTC Loan Activity rows now render as designed.

## Review Scope

**Explicit must-fix:**

**Production code:**
- `src/tax_reporting/application/crypto/aggregation.py` - `_aggregate_capital_entries`, `_re_evaluate_aggregated_review` (new private helper inlined here), `_aggregate_ogr_validation` (read-only context)
- `src/tax_reporting/application/crypto/loan_activity.py` - `_extract_loan_activity`; module-level `LOAN_OVERSHOOT_INTEREST_PCT` constant
- `src/tax_reporting/application/crypto/entities.py` - `LoanActivityEntry` (add `balance_detail: str | None` field)
- `src/tax_reporting/application/persisting/loan_activity_sheet.py` - `write_loan_activity_sheet` (sentinel-based fill-color mapping; new sibling "Balance Detail" column 10; extend fill loop to `range(1, 11)`)
- `src/tax_reporting/domain/constants.py` - five loan-status sentinel constants (all five new; `LOAN_STATUS_OVERPAID` deleted outright, superseded by `LOAN_STATUS_OVERPAID_VERIFY` with a different value)

**Tests:**
- `tests/unit/application/test_crypto_reporting.py` - extend `_aggregate_capital_entries` test block; add `TestReEvaluateAggregatedReview` class for the inlined filter helper; add `TestExtractLoanActivityClassification` class calling `_extract_loan_activity` via TH CSV fixtures (covers the five-sentinel classifier - four Invariant-4 branches plus the unchanged Settled/Open-loan branches); re-scope `test_extract_loan_activity_overpaid_status` (line 6370-6383)
- `tests/unit/application/persisting/test_loan_activity_sheet.py` - re-scope `TestLoanActivitySheetPerAssetBalance` (line 87-133), `TestLoanActivitySheetOverpaid` (line 137-156), `TestLoanActivitySheetSettled` (line 200-228), AND `TestLoanActivitySheetOpenLoan` (line 160-182); add NEW `TestLoanActivitySheetNoEurPrice` and `TestLoanActivitySheetInAssetInterest` classes (six classes total - see Task 2 body for per-class fixture and assertion deltas)

**Plan-related extension:** standard policy - review may touch other files where the change is causally linked (e.g. assumptions-sheet rendering if it surfaces review_reason text, or integration tests that assert on the old strings).

**Out of scope; reject unless plan-related:**
- `src/tax_reporting/application/crypto/fifo_helpers.py` - `_apply_phantom_lot_flags` and `_build_zero_basis_review_reason` logic is unchanged; only its caller in `_aggregate_capital_entries` is changed.
- `src/tax_reporting/application/crypto_reporting.py:681-799` (`_parse_capital_gains_file` body) - lot-level parsing unchanged.
- FEE-token-lot filtering at the lot parser - deferred; see `docs/history/feature-notes/2026-07-15-review-flag-deferred-findings.md`.
- Reward-FMV-not-carried-into-basis detection - deferred; same note.

## Design Invariants (CR Guard)

1. **Per-lot signal preservation.** The lot-level `review_required` / `review_reason` on `CryptoCapitalGainEntry` and `CryptoFifoRealization` MUST remain set as today; this plan only changes how the *aggregated* row's flag is computed. The per-lot review list (`context.review_entries`) and WARNING logs are not silenced.
2. **Filter owns both aggregated fields atomically.** `_re_evaluate_aggregated_review` returns `(review_required, review_reason)` and `_aggregate_capital_entries` MUST apply BOTH fields from the filter's return in a single `replace(...)` call. `CryptoCapitalGainEntry.__post_init__` (`entities.py:390-393`) raises `ValueError` on the inconsistent pair `review_required=True AND review_reason=None`; partial application of the filter (e.g. updating `review_reason` but leaving `review_required` from the `any(...)` reduction) crashes the pipeline. The filter is the single source of truth for both fields on the aggregated entry. The helper is inlined as a private function inside `aggregation.py` (single caller, reuses `_MATERIALITY_THRESHOLD` without a cross-module import). **The helper body MUST start with `if entry.review_reason is None: return (entry.review_required, entry.review_reason)`**  -  the materiality gate depends only on numeric fields, and the aggregated `review_reason` is `None` on the default clean-disposal path (every lot has `review_required=False`, producing `"; ".join(...) or None` = `None` at `aggregation.py:331`). Without this guard, `None.split("; ")` crashes the pipeline on the first material clean disposal. Dedicated test: `test_none_review_reason_with_material_values_returns_unchanged`.
3. **Materiality gate is the existing `_MATERIALITY_THRESHOLD = Decimal("1")`.** Do not introduce a second materiality constant; reuse the same one that `_filter_immaterial_entries` uses.
4. **Loan Activity classification uses amount-space overshoot, not EUR-space.** EUR values are 0 for LBTC and any asset Koinly didn't price. The percentage is `abs(repaid_amount − received_amount) / received_amount × 100`. The classifier has FOUR branches evaluated in this exact precedence order: (b) FIRST `received_amount == 0 AND repaid_amount > 0` → `LOAN_STATUS_OVERPAID_VERIFY` (light-red; repayment-only asset - overshoot is unbounded; this wins over (a) because a repayment-only asset is the stronger anomaly even when no EUR price exists); (a) THEN `received_value_eur == 0` → `LOAN_STATUS_NO_EUR_PRICE` (yellow); (c) THEN `overshoot_pct <= LOAN_OVERSHOOT_INTEREST_PCT` → `LOAN_STATUS_IN_ASSET_INTEREST` (neutral); (d) OTHERWISE → `LOAN_STATUS_OVERPAID_VERIFY` (light-red). Note: a repayment-only asset DOES enter `accs` via the loan-repayment branch starting at `loan_activity.py:71` (the `elif tag == "loan repayment"` block; accumulator update at `:99-103`), so branch (b) is reachable - the prior claim "the row would not exist in `accs` in that case" was wrong.
5. **`LOAN_OVERSHOOT_INTEREST_PCT = Decimal("1")` is a module-level constant in `loan_activity.py` (no underscore, no re-export).** Tests import it directly from `loan_activity.py`. Do not put it in `domain/constants.py`; it is a classification-layer threshold consumed by `_extract_loan_activity`, co-located with its sole production caller. `domain/constants.py` is reserved for cross-module business constants. Per CLAUDE.md "never introduce a hardcoded value without first flagging it" - flagged at design time, user-approved.
6. **`balance_status` is a sentinel; varying text goes in `balance_detail`.** The Excel sheet's fill-color mapping matches `entry.balance_status == LOAN_STATUS_<BUCKET>` (exact equality against a constant). The overshoot percentage is rendered from `entry.balance_detail` (e.g. `"overshoot 0.5853%"`) into a sibling cell, NOT interpolated into `balance_status`. This decouples classification semantics from presentation severity.

## Validation Commands

```bash
# Targeted: the two changed areas
uv run pytest tests/unit/application/test_crypto_reporting.py \
  tests/unit/application/persisting/test_loan_activity_sheet.py -v

# Broader non-regression
uv run pytest -m unit

# Lint touched files
uv run ruff check \
  src/tax_reporting/application/crypto/aggregation.py \
  src/tax_reporting/application/crypto/loan_activity.py \
  src/tax_reporting/application/crypto/entities.py \
  src/tax_reporting/application/persisting/loan_activity_sheet.py \
  src/tax_reporting/domain/constants.py \
  tests/unit/application/test_crypto_reporting.py \
  tests/unit/application/persisting/test_loan_activity_sheet.py

# Stale-string sweep: per development_lessons.md #55, scan src/tests AND docs
# directories AND README for the old label text and the "cross-year loan" phrase
grep -rn "Overpaid (cross-year loan?)" src/ tests/ docs/maintenance/ docs/architecture/ README.md
grep -rn "Zero EUR value for known crypto asset" src/ tests/ docs/maintenance/ docs/architecture/ README.md
grep -rn "cross-year loan" src/ tests/ docs/maintenance/ docs/architecture/ README.md
```

The grep sweep is contract-removal validation. After the plan lands, the exact old label `"Overpaid (cross-year loan?)"` (note the closing paren immediately after the question mark) MUST appear NOWHERE (zero-hit expectation); the new sentinel `"Overpaid (cross-year loan? verify)"` (closing paren after "verify") does not contain the old literal as a substring, so it is not a false positive. The broader `cross-year loan` substring sweep (third command) is the one that legitimately still matches the new sentinel constant definition. The `docs/` and `README.md` scan is mandated by `development_lessons.md` #55 (deletion-verification grep backstops must scan `docs/maintenance/`, `docs/architecture/`, `README.md`).

### Task 1 - Crypto Gains: re-evaluate review flag on aggregated disposal rows

Files:
- `src/tax_reporting/application/crypto/aggregation.py` - inline new private helper `_re_evaluate_aggregated_review(entry) -> tuple[bool, str | None]` next to `_aggregate_capital_entries`; wire it into the aggregated-entry construction
- `tests/unit/application/test_crypto_reporting.py` - new `TestReEvaluateAggregatedReview` class for the helper; extend the `_aggregate_capital_entries` test block

- [x] `test_crypto_reporting.py#TestReEvaluateAggregatedReview#test_strips_zero_basis_when_aggregated_values_material`; given an entry with `cost_eur=100, proceeds_eur=200, gain_loss_eur=100` and `review_reason` carrying the three zero-basis prefixes, expects `_re_evaluate_aggregated_review` to return `(False, None)`
- [x] `test_crypto_reporting.py#TestReEvaluateAggregatedReview#test_preserves_zero_basis_when_all_lots_zero`; given an entry with `cost_eur=0, proceeds_eur=0, gain_loss_eur=0` and a zero-basis reason, expects the reason returned unchanged (signal preserved when the whole disposal really is suspect)
- [x] `test_crypto_reporting.py#TestReEvaluateAggregatedReview#test_none_review_reason_with_material_values_returns_unchanged`; given an entry with `cost_eur=10, proceeds_eur=20, gain_loss_eur=10, review_reason=None`, expects `_re_evaluate_aggregated_review` to return `(False, None)` WITHOUT raising `AttributeError`. Pins the None-guard on the default clean-disposal path (the most common production case where every lot has `review_required=False`, producing `review_reason=None` via `"; ".join(...) or None` at `aggregation.py:331`). Without this guard `None.split("; ")` crashes the pipeline on the first material clean disposal.
- [x] `test_crypto_reporting.py#TestReEvaluateAggregatedReview#test_preserves_zero_basis_when_only_cost_is_zero`; given `cost_eur=0, proceeds_eur=200, gain_loss_eur=100, review_reason="Zero acquisition cost…"` (only the cost clause is zero; proceeds and gain are material), expects the filter to preserve the reason. Pins the `cost_eur > 0` clause of the gate independently.
- [x] `test_crypto_reporting.py#TestReEvaluateAggregatedReview#test_preserves_zero_basis_when_only_proceeds_are_zero`; given `cost_eur=100, proceeds_eur=0, gain_loss_eur=100, review_reason="Zero disposal proceeds…"` (only the proceeds clause is zero), expects the filter to preserve the reason. Pins the `proceeds_eur > 0` clause of the gate independently.
- [x] `test_crypto_reporting.py#TestReEvaluateAggregatedReview#test_preserves_unrelated_reasons`; parametrized over `[phantom_lot_reason, operator_origin_reason, homoglyph_reason, missing_cost_basis_with_impact_reason, foreign_tax_parse_reason, ogr_override_reason]`, given an entry carrying only that reason with material aggregated values, expects the reason returned unchanged
- [x] `test_crypto_reporting.py#TestReEvaluateAggregatedReview#test_strips_at_exact_materiality_boundary`; given `cost_eur=10, proceeds_eur=11, gain_loss_eur=Decimal("1")` (exactly at `_MATERIALITY_THRESHOLD`) and a zero-basis reason, expects the filter to strip and return `(False, None)`. Pins the inclusive `>=` boundary.
- [x] `test_crypto_reporting.py#TestReEvaluateAggregatedReview#test_preserves_when_gain_just_below_materiality`; given `cost_eur=10, proceeds_eur=10.99, gain_loss_eur=Decimal("0.99")` (one cent below the threshold) and a zero-basis reason, expects the reason preserved (materiality gate fails).
- [x] `test_crypto_reporting.py#TestReEvaluateAggregatedReview#test_strips_each_zero_basis_prefix_individually`; parametrized over THREE cases - the two `fifo_helpers` constants (`_ZERO_COST_REASON`, `_ZERO_PROCEEDS_REASON`) PLUS a synthetic literal starting with `"Zero EUR value for known crypto asset"` (e.g. `"Zero EUR value for known crypto asset - test fixture"`) which originates from `crypto_reporting.py:739`, NOT from `fifo_helpers`, and is therefore not exercised by the two imported constants alone. Given an entry carrying only that reason with material aggregated values, expects the filter to return `(False, None)`. Verifies the split-and-prefix-match logic recognizes each prefix after splitting on `"; "`. (Symbols imported per the consolidated directive in Task 2.)
- [x] `test_crypto_reporting.py#TestReEvaluateAggregatedReview#test_joined_reason_partial_strip`; parametrized over TWO cases. (i) Single survivor: `review_reason` joining one zero-basis reason with `"Phantom lot: …"` expects only the phantom-lot reason kept (separator-normalized to `"; "`). (ii) Multi-survivor: `review_reason` joining `"Phantom lot: …"`, `"Operator origin review: …"`, AND `"Zero acquisition cost …"` (3 parts, 2 non-zero-basis survivors + 1 zero-basis) expects the result to preserve BOTH non-zero-basis survivors in insertion order (`"Phantom lot: …; Operator origin review: …"`). A regression returning `[surviving[0]]` instead of `surviving` fails case (ii) but passes (i).
- [x] Run → expect RED: `uv run pytest tests/unit/application/test_crypto_reporting.py -v -k "ReEvaluateAggregatedReview"`
- [x] Implement `_re_evaluate_aggregated_review` as a private function in `aggregation.py` (next to `_aggregate_capital_entries`). Body MUST start with a None guard: `if entry.review_reason is None: return (entry.review_required, entry.review_reason)`  -  this is the default production case for a clean aggregated disposal (every lot has `review_required=False`, producing `review_reason=None`), and without the guard the materiality gate below would dereference `None.split("; ")` and crash. After the None guard, evaluate the gate: when `entry.cost_eur > 0 AND entry.proceeds_eur > 0 AND abs(entry.gain_loss_eur) >= _MATERIALITY_THRESHOLD`, split `entry.review_reason` on `"; "`, drop any part that `startswith` one of the zero-basis prefixes (`"Zero EUR value for known crypto asset"`, `"Zero acquisition cost"`, `"Zero disposal proceeds"`), and return `(False, None)` if no parts survive OR `(True, "; ".join(surviving))` if some survive. Otherwise (gate fails OR review_reason is None) return `(entry.review_required, entry.review_reason)` unchanged. Define the prefix tuple as a module-level `_ZERO_BASIS_REASON_PREFIXES: Final[tuple[str, ...]]` constant in `aggregation.py`; this replaces the prior design's full-string-equality strip set and survives minor upstream-prose edits without a regression-guard test.
- [x] Run → expect GREEN for the helper unit tests
- [x] `tests/unit/application/test_crypto_reporting.py#test_mixed_lot_aggregated_drops_zero_basis_reason` (new test in the `_aggregate_capital_entries` test block, NOT a class); given a 5-entry group where one lot has `cost=0, proceeds=0, gain=0, review_required=True, review_reason="Zero EUR value…"` and the other four lots are clean, expects the aggregated entry's `review_required=False, review_reason=None`. Cite Invariant 1 + 2.
- [x] `tests/unit/application/test_crypto_reporting.py#test_all_zero_lot_group_preserves_zero_basis_reason` (new test); given a 3-entry group where every lot has all-zero values, expects the aggregated `review_required=True` and a reason containing `"Zero EUR value…"`
- [x] `tests/unit/application/test_crypto_reporting.py#test_phantom_lot_reason_survives_mixed_aggregation` (new test); given a group where one lot carries a phantom-lot reason and another lot is all-zero, expects the aggregated reason to retain the phantom-lot text and drop the zero-basis text
- [x] Wire the helper into `_aggregate_capital_entries` (`aggregation.py:322-337`): after the existing `replace(...)` builds the aggregated entry, call `_re_evaluate_aggregated_review(aggregated_entry)` and apply BOTH fields from the return in a single `replace(...)` call (per Invariant 2: helper is the single source of truth for both fields; partial application triggers `ValueError` from `CryptoCapitalGainEntry.__post_init__`)
- [x] Run → expect GREEN: `uv run pytest tests/unit/application/test_crypto_reporting.py -v -k "aggregate or ReEvaluateAggregatedReview"`
- [x] Commit: `feat(crypto): re-evaluate zero-basis review flag at aggregation boundary` (commit f5e606f)
- [ ] Manual: re-run on user's 2025 export; confirm the four flagged Crypto Gains rows now show `NO` when their aggregated values are material (DEFERRED to user; requires gitignored personal 2025 export; specific asset/wallet/date identifiers omitted here as personal portfolio data)

### Task 2 - Loan Activity: five-sentinel classification (four classifier branches) replacing the single "Overpaid" label

Files:
- `src/tax_reporting/domain/constants.py`
- `src/tax_reporting/application/crypto/entities.py` - `LoanActivityEntry` (add `balance_detail: str | None = None`)
- `src/tax_reporting/application/crypto/loan_activity.py` - `_extract_loan_activity`; module-level `LOAN_OVERSHOOT_INTEREST_PCT = Decimal("1")`
- `src/tax_reporting/application/persisting/loan_activity_sheet.py` - sentinel-based fill-color mapping; new sibling "Balance Detail" column 10; extend fill loop to `range(1, 11)`
- `tests/unit/application/test_crypto_reporting.py` - new `TestExtractLoanActivityClassification` class calling `_extract_loan_activity` via TH CSV fixtures (covers the five-sentinel classifier - four Invariant-4 branches plus the unchanged Settled/Open-loan branches); re-scope `test_extract_loan_activity_overpaid_status` (line 6370-6383)
- `tests/unit/application/persisting/test_loan_activity_sheet.py` - re-scope `TestLoanActivitySheetPerAssetBalance`, `TestLoanActivitySheetOverpaid`, `TestLoanActivitySheetSettled`, AND `TestLoanActivitySheetOpenLoan`; add NEW `TestLoanActivitySheetNoEurPrice` and `TestLoanActivitySheetInAssetInterest` classes (six classes total - all assert via constants and the new sibling column 10). See body for per-class fixture and assertion deltas.

NOTE on layering: classification-correctness tests (`TestExtractLoanActivityClassification`) live in `test_crypto_reporting.py` and call `_extract_loan_activity` via TH CSV fixtures (matching the existing `test_extract_loan_activity_overpaid_status` pattern at line 6370). The sheet test file tests `write_loan_activity_sheet` rendering with pre-constructed `LoanActivityEntry` fixtures; it does not import or invoke `_extract_loan_activity`, so it exercises zero classifier branches. Branch coverage must come from the classification tests; the sheet tests only guard rendering.

- [x] `test_crypto_reporting.py#TestExtractLoanActivityClassification#test_small_overshoot_with_eur_classified_as_in_asset_interest`; parametrized over TWO cases. (i) Production-shape fixture: `received_value_eur=4697.96, repaid_value_eur=4712.19, received_amount=Decimal("0.06151"), repaid_amount=Decimal("0.06187")`; expects `balance_status == LOAN_STATUS_IN_ASSET_INTEREST` and `balance_detail == "overshoot 0.5853%"` (`abs(0.06187 - 0.06151) / 0.06151 * 100 = 0.585270…%`, 4dp). (ii) Discriminating fixture: `received_amount=Decimal("10000"), repaid_amount=Decimal("10058.525")` (and matching EUR values `received_value_eur > 0`); raw_pct = 0.58525%; expects `balance_detail == "overshoot 0.5853%"`. The 5th-decimal digit is exactly 5 and the predecessor (2) is even, so `ROUND_HALF_UP` gives 0.5853 and `ROUND_HALF_EVEN` gives 0.5852; case (ii) is the one that actually pins the rounding mode. The earlier `0.58%` assertion was arithmetically wrong (no standard rounding produces it).
- [x] **Consolidated import directive for `tests/unit/application/test_crypto_reporting.py` (covers r9 #4 + r10 H2 in one place to avoid two directives touching the same block): after the existing aggregation import block at `:12-19`, add (a) `from tax_reporting.application.crypto.fifo_helpers import _ZERO_COST_REASON, _ZERO_PROCEEDS_REASON` (Task 1's `test_strips_each_zero_basis_prefix_individually` parametrize references these symbols), (b) `from tax_reporting.application.crypto.loan_activity import LOAN_OVERSHOOT_INTEREST_PCT`, and (c) `from tax_reporting.domain.constants import LOAN_STATUS_SETTLED, LOAN_STATUS_OPEN_LOAN, LOAN_STATUS_IN_ASSET_INTEREST, LOAN_STATUS_NO_EUR_PRICE, LOAN_STATUS_OVERPAID_VERIFY`. The new `TestReEvaluateAggregatedReview` and `TestExtractLoanActivityClassification` classes reference these symbols; without the imports the module fails at collection time with `NameError`. The existing `test_extract_loan_activity_overpaid_status` re-scope (below) also needs the sentinels.**
- [x] **Decorate both new test classes `TestReEvaluateAggregatedReview` (Task 1) and `TestExtractLoanActivityClassification` (Task 2) with `@pytest.mark.unit` so the release-gate command `uv run pytest -m unit` collects them. The file `tests/unit/application/test_crypto_reporting.py` is inconsistent - some classes carry the mark, some do not - so an implementer who copy-pastes from an unmarked class would silently skip the headline tests under the `-m unit` gate.**
- [x] `test_crypto_reporting.py#TestExtractLoanActivityClassification#test_no_eur_price_classified_as_cannot_classify`; given a TH CSV fixture with `received_amount > 0 AND repaid_amount > 0 AND received_value_eur == 0 AND repaid_value_eur == 0`, expects `balance_status == LOAN_STATUS_NO_EUR_PRICE`. The explicit `received_amount > 0` is load-bearing: per Invariant 4 branch (b) (`received_amount == 0 AND repaid_amount > 0`) is evaluated FIRST and routes to `LOAN_STATUS_OVERPAID_VERIFY`, so a fixture with `received_amount == 0` would go RED for the wrong reason.
- [x] `test_crypto_reporting.py#TestExtractLoanActivityClassification#test_large_overshoot_routes_to_overpaid_verify`; given a TH CSV fixture with `received_amount=Decimal("100"), repaid_amount=Decimal("105")` (5.0% overshoot) and `received_value_eur > 0`, expects `balance_status == LOAN_STATUS_OVERPAID_VERIFY` (assert via the constant symbol, not the literal string). Follow the TH CSV column layout used by `test_extract_loan_activity_with_settled_loan` at line 6286 (and the existing `test_extract_loan_activity_overpaid_status` at line 6370-6383): receipt row has `Net Value (EUR)` populated so `received_value_eur > 0` after parsing; the `_write_transaction_history` helper at line 6272 sets the column shape.
- [x] `test_crypto_reporting.py#TestExtractLoanActivityClassification#test_overshoot_boundary_1pct_inclusive`; parametrized over three cases (`received=100, repaid=100.99` → IN_ASSET_INTEREST; `received=100, repaid=101` → IN_ASSET_INTEREST; `received=100, repaid=101.01` → OVERPAID_VERIFY) AND all three cases carrying `received_value_eur > 0` (Net Value (EUR) > 0 on the receipt row; branch (a) `received_value_eur == 0 → LOAN_STATUS_NO_EUR_PRICE` fires BEFORE branch (c), so a fixture with `Net Value (EUR)=0` routes all three cases to `NO_EUR_PRICE` and the test goes RED for the wrong reason), expects the boundary predicate `overshoot_pct <= LOAN_OVERSHOOT_INTEREST_PCT` to be inclusive at exactly 1.00%. Covers all three boundary cells so a `<=` vs `<` regression OR an off-by-one in percentage computation is caught.
- [x] `test_crypto_reporting.py#TestExtractLoanActivityClassification#test_repayment_only_asset_routes_to_overpaid_verify`; given a TH CSV fixture with `received_amount == 0 AND repaid_amount > 0 AND received_value_eur == 0 AND repaid_value_eur == 0`, expects `balance_status == LOAN_STATUS_OVERPAID_VERIFY` (and `balance_detail == "received_amount=0; repayment-only asset"`). Per Invariant 4, branch (b) (`received_amount == 0 AND repaid_amount > 0`) is evaluated FIRST and short-circuits before branch (a) checks `received_value_eur == 0`; therefore `received_value_eur == 0` has NO effect on a repayment-only row. (Folded from r5 simplification finding #13: the prior standalone duplicate `test_received_zero_and_no_eur_price_routes_to_overpaid_verify_not_cannot_classify` is dropped; its precedence-discrimination intent is preserved here.)
- [x] `test_crypto_reporting.py#TestExtractLoanActivityClassification#test_settled_and_open_loan_unchanged`; given TH CSV fixtures producing `balance == ZERO` (Settled) and `balance > ZERO` (Open loan), expects `balance_status` to be `LOAN_STATUS_SETTLED` / `LOAN_STATUS_OPEN_LOAN` respectively and `balance_detail is None` (regression guard for the unchanged branches).
- [x] Run → expect RED: `uv run pytest tests/unit/application/test_crypto_reporting.py -v -k "ExtractLoanActivityClassification"`
- [x] In `domain/constants.py`: add sentinels `LOAN_STATUS_SETTLED = "Settled"`, `LOAN_STATUS_OPEN_LOAN = "Open loan"`, `LOAN_STATUS_IN_ASSET_INTEREST = "Likely in-asset interest"`, `LOAN_STATUS_NO_EUR_PRICE = "Cannot classify: no EUR price data"`, `LOAN_STATUS_OVERPAID_VERIFY = "Overpaid (cross-year loan? verify)"`. **Delete `LOAN_STATUS_OVERPAID` outright** - pre-revision grep confirmed the only callers are `loan_activity.py:10,112` and `loan_activity_sheet.py:13,75` (both in this plan's scope and rewritten by this task); no external test imports the symbol (tests hardcode the literal string, which the test-rewrite steps below address).
- [x] In `entities.py:LoanActivityEntry`: add field `balance_detail: str | None = None` after `balance_status`. Default None preserves backward compatibility for existing construction sites (the `_make_loan_activity_entry` helper at `test_loan_activity_sheet.py:18-31` and the production site at `loan_activity.py:115-126` both continue to work unchanged - `defaults.update(overrides)` accepts the new key when provided, and the dataclass default `None` applies when omitted).
- [x] In `loan_activity.py`: add module-level `LOAN_OVERSHOOT_INTEREST_PCT = Decimal("1")` (no underscore; tests import directly from this module per Invariant 5). Refactor the status branch (lines 109-114): the `if balance == ZERO` (Settled) and `else` (Open loan) branches remain as-is; only the `elif balance < ZERO:` body is replaced. Inside that elif body, compute `raw_pct = abs(repaid_amount - received_amount) / received_amount * 100 if received_amount > 0 else None` and select `balance_status` / `balance_detail` per Invariant 4's four branches (b) → (a) → (c) → (d). **Branch (b) sets `balance_detail = "received_amount=0; repayment-only asset"` directly (do NOT call `.quantize(...)` on `raw_pct` for this branch - `raw_pct is None` here because `received_amount == 0`, and `None.quantize(...)` crashes); the quantize/ROUND_HALF_UP pin below applies ONLY to branches (c) and (d) where `raw_pct` is non-None.** **For the `balance_detail` percentage rendering on branches (c)/(d), pin the rounding explicitly: `from decimal import ROUND_HALF_UP`; `pct = raw_pct.quantize(Decimal("0.0001"), rounding=ROUND_HALF_UP)`; `balance_detail = f"overshoot {pct}%"`.** Python's default `Decimal.quantize()` uses `ROUND_HALF_EVEN` (banker's rounding); production data will eventually hit a value whose 5th-decimal-place digit is exactly 5 with an even predecessor, where `ROUND_HALF_EVEN` and `ROUND_HALF_UP` diverge. The fixture `0.585270...%` rounds to `0.5853%` under both modes (4dp), so the prescribed test alone does not discriminate; pin the mode in the implementation step. **Replace the inline literals `"Settled"` and `"Open loan"` at `loan_activity.py:110,114` with `LOAN_STATUS_SETTLED` and `LOAN_STATUS_OPEN_LOAN` (imported from `domain.constants`); the new sentinels exist precisely so the sheet matches against symbols, not literals - leaving the inline string in the producing site defeats the purpose.** **Update the import at `loan_activity.py:10` to bring in all five sentinels (`LOAN_STATUS_SETTLED`, `LOAN_STATUS_OPEN_LOAN`, `LOAN_STATUS_IN_ASSET_INTEREST`, `LOAN_STATUS_NO_EUR_PRICE`, `LOAN_STATUS_OVERPAID_VERIFY`); drop the `LOAN_STATUS_OVERPAID` import (deleted in the constants step).** `Settled` and `Open loan` branches both also leave `balance_detail=None`. The ternary `if received_amount > 0 else None` guards the division-by-zero; branch (b)'s predicate (`received_amount == 0 AND repaid_amount > 0`) is checked FIRST and short-circuits before any division.
- [x] In `loan_activity_sheet.py:write_loan_activity_sheet`: change the fill mapping to match `entry.balance_status` against the sentinel constants (NOT the rendered string). `LOAN_STATUS_NO_EUR_PRICE` → yellow; `LOAN_STATUS_OVERPAID_VERIFY` → light-red; `LOAN_STATUS_IN_ASSET_INTEREST` → no fill; `LOAN_STATUS_SETTLED`/`LOAN_STATUS_OPEN_LOAN` → no fill. **Define BOTH fills as module-level constants side-by-side: promote the existing function-scoped `red_fill` at `loan_activity_sheet.py:62` to a module-level `_LIGHT_RED_FILL = PatternFill(start_color="FFFFCCCC", end_color="FFFFCCCC", fill_type="solid")` (same hex as the existing literal; do NOT change the color), AND add a new module-level `_NO_EUR_PRICE_FILL = PatternFill(start_color="FFFFFF00", end_color="FFFFFF00", fill_type="solid")` (yellow; mirrors the existing `_YELLOW_FILL` at `crypto_gains_sheet.py:21`); the two fills must live next to each other so the symmetry is visible. Do NOT pick a different yellow or light-red hex on the fly. **Delete the function-scoped `red_fill = PatternFill(...)` at `loan_activity_sheet.py:62`; replace its sole usage at line 77 with `_LIGHT_RED_FILL`.**** Add a new sibling "Balance Detail" column (column 10) after "Balance Status"; write `safe_cell_value(entry.balance_detail)` to column 10 when non-None, else leave blank (wrapping via the existing `safe_cell_value()` helper matches the module convention at lines 65, 72, 75, 76, 82 and closes the latent CSV-injection vector if a future classifier edit interpolates a CSV-derived field). **Extend the fill loop at `loan_activity_sheet.py:76` from `range(1, 10)` to `range(1, 11)`** so yellow/light-red fills cover the new column. Do NOT append `balance_detail` to the Balance Status cell - Invariant 6 requires the fill-color match against the sentinel constant alone, and co-mingling text would break exact-equality matching.
- [x] Update the function docstring at `loan_activity_sheet.py:18-30` and the inline comment at `:37-40` (currently describe only the single "Overpaid (cross-year loan?)" case) to describe the five loan-status sentinels and their three fill colors (neutral, yellow, light-red) applied across four behavior branches (the OVERPAID_VERIFY sentinel arises from two distinct classifier branches - large-overshoot and repayment-only - both mapped to light-red). The grep sweep at Validation Commands does NOT catch these because the prose uses "cross-year repayment" / "prior-year loan" rather than the literal `"Overpaid (cross-year loan?)"` pattern; they must be updated explicitly.
- [x] Update the sheet-level note (R1 cell, currently mentions cross-year only) to describe the five loan-status sentinels (`Settled` / `Open loan` / `Likely in-asset interest` / `Cannot classify: no EUR price data` / `Overpaid (verify)`) and their fills (neutral / neutral / neutral / yellow / light-red).
- [x] **Update the import at `loan_activity_sheet.py:13` to bring in all five sentinel constants (`LOAN_STATUS_SETTLED`, `LOAN_STATUS_OPEN_LOAN`, `LOAN_STATUS_IN_ASSET_INTEREST`, `LOAN_STATUS_NO_EUR_PRICE`, `LOAN_STATUS_OVERPAID_VERIFY`) from `tax_reporting.domain.constants`; drop the `LOAN_STATUS_OVERPAID` import (deleted in the constants step of this task). Without this directive the fill-mapping branch at `loan_activity_sheet.py:write_loan_activity_sheet` raises `NameError` on every `LOAN_STATUS_*` reference.** (r9 finding #4)
- [x] Update `tests/unit/application/persisting/test_loan_activity_sheet.py:TestLoanActivitySheetOverpaid` (line 137-156) - the existing test asserts the literal `"Overpaid (cross-year loan?)"` string; re-scope it to assert `entry.balance_status == LOAN_STATUS_OVERPAID_VERIFY`. **Set `balance_detail="overshoot 5.0000%"` on the fixture explicitly** (production branch (d) emits a non-None `balance_detail`; without this the column-10 assertion becomes `None == None` and cannot detect a regression that drops the column-10 write). Bump the fill-check loop from `range(1, 10)` to `range(1, 11)` AND assert `ws.cell(data_row, 10).value == entry.balance_detail` so a regression that drops the production column-10 write or writes to the wrong column fails this test.
- [x] **Update the imports in `tests/unit/application/persisting/test_loan_activity_sheet.py` to bring in all five sentinel constants from `tax_reporting.domain.constants` (the re-scoped `TestLoanActivitySheetOverpaid`, the new `TestLoanActivitySheetNoEurPrice`, and the new `TestLoanActivitySheetInAssetInterest` all reference these symbols by name; without the import the module fails at collection time). Decorate the two new classes (`TestLoanActivitySheetNoEurPrice`, `TestLoanActivitySheetInAssetInterest`) with `@pytest.mark.unit` matching the existing `@pytest.mark.unit` decorators at `:86/136/159/199` (decorator lines; the class declarations are at `:87/137/160/200`).**
- [x] Add NEW `tests/unit/application/persisting/test_loan_activity_sheet.py:TestLoanActivitySheetNoEurPrice#test_loan_activity_flags_no_eur_price_yellow`; given a fixture with `balance_status=LOAN_STATUS_NO_EUR_PRICE` and `balance_detail=None`, render the sheet and assert a yellow fill (`start_color=="FFFFFF00"`) is applied across `range(1, 11)` via a new `_is_yellow_fill(cell)` helper (sibling to the existing `_is_light_red_fill` at `:77-83`). Also assert `ws.cell(data_row, 10).value is None` (no balance_detail on this branch). The yellow fill is a brand-new visual artifact in this plan; without these tests, a regression that drops the `LOAN_STATUS_NO_EUR_PRICE` branch from the fill mapping passes every other prescribed test.
- [x] Add NEW `TestLoanActivitySheetInAssetInterest#test_loan_activity_in_asset_interest_no_fill_and_renders_detail`; given a fixture with `balance_status=LOAN_STATUS_IN_ASSET_INTEREST` and `balance_detail="overshoot 0.5853%"`, render the sheet and assert (i) no fill is applied across `range(1, 11)` for any cell, AND (ii) `ws.cell(data_row, 9).value == LOAN_STATUS_IN_ASSET_INTEREST` (the sentinel in column 9, NOT clobbered by balance_detail), AND (iii) `ws.cell(data_row, 10).value == "overshoot 0.5853%"` (balance_detail rendered in the sibling column 10). Pins both the no-fill branch and the column-10 value-rendering contract.
- [x] Update `tests/unit/application/persisting/test_loan_activity_sheet.py:TestLoanActivitySheetSettled#test_loan_activity_settled_no_highlight` (decorator at `:199`, class at `:200-228`, method at `:203`) - bump the fill-check loop from `range(1, 10)` to `range(1, 11)` AND assert `ws.cell(data_row, 10).value is None` (Settled rows have no balance_detail). Add `TestLoanActivitySheetSettled` to the rewrite scope (the Task 2 Files list named only `TestLoanActivitySheetPerAssetBalance` and `TestLoanActivitySheetOverpaid`); the Settled class uses the same fill loop and needs the same bump.
- [x] Update `tests/unit/application/persisting/test_loan_activity_sheet.py:TestLoanActivitySheetOpenLoan#test_loan_activity_flags_open_loan` (existing class at `:160-182`, existing method at `:163`) - the existing test body has NO fill-check loop (it only asserts `ws.cell(data_row, 9).value == "Open loan"` and `ws.cell(data_row, 8).value == Decimal("400")` at `:181-182`); ADD a new no-fill assertion loop `for col in range(1, 11): assert not _is_light_red_fill(ws.cell(data_row, col)) and not _is_yellow_fill(ws.cell(data_row, col))` AND assert `ws.cell(data_row, 10).value is None` (Open loan rows have no balance_detail). Add `TestLoanActivitySheetOpenLoan` to the rewrite scope. Without this, a regression that paints yellow or light-red on Open Loan rows passes every prescribed test.
- [x] Update `tests/unit/application/persisting/test_loan_activity_sheet.py:TestLoanActivitySheetPerAssetBalance` (line 87-133) - the WBTC fixture at line 103-113 is constructed with `received_amount=Decimal("0"), repaid_amount=Decimal("0.031")` and `balance_status="Overpaid (cross-year loan?)"` set directly in the fixture. This is a SHEET-RENDERING test: it calls `write_loan_activity_sheet` with a pre-built entry and does NOT invoke `_extract_loan_activity`, so it exercises zero classifier branches. Leave the fixture numbers as-is; change only the `balance_status` argument from the literal `"Overpaid (cross-year loan?)"` to `LOAN_STATUS_OVERPAID_VERIFY` (import from `domain.constants`) and assert via the constant symbol at line 133. Branch-(b) classification coverage is provided by `TestExtractLoanActivityClassification#test_repayment_only_asset_routes_to_overpaid_verify` in `test_crypto_reporting.py`, not by this sheet test. Blocker finding 1 + r2 finding 3.
- [x] Update `tests/unit/application/test_crypto_reporting.py:test_extract_loan_activity_overpaid_status` (line 6370-6383) - the existing TH CSV fixture (receipt 50.00 SUI at line 6377, repayment 100.00 SUI at line 6378) is already a 100% overshoot, which satisfies Invariant 4's branch (d) (`overshoot_pct > LOAN_OVERSHOOT_INTEREST_PCT` → `LOAN_STATUS_OVERPAID_VERIFY`). Re-scope the assertion at line 6383 from the literal `"Overpaid (cross-year loan?)"` to `entries[0].balance_status == LOAN_STATUS_OVERPAID_VERIFY` (import from `domain.constants`); no fixture change needed. Rename to `test_extract_loan_activity_overpaid_verify_status`. Blocker finding 1 + r2 finding 4.
- [x] Run → expect GREEN: `uv run pytest tests/unit/application/test_crypto_reporting.py -v -k "ExtractLoanActivityClassification or overpaid_verify" tests/unit/application/persisting/test_loan_activity_sheet.py -v`
- [x] Commit: `feat(crypto): replace single Overpaid loan status with five sentinels (settled / open / in-asset-interest / no-EUR-price / overpaid-verify)`

### Task 3 - Documentation updates

Files:
- `docs/maintenance/glossary.md`
- `docs/maintenance/crypto_reporting_guidelines.md`
- `docs/maintenance/crypto_implementation_guidelines.md`
- `docs/maintenance/koinly_guidelines.md`
- `docs/maintenance/tax_reporting_guidelines.md`

- [x] `docs/maintenance/glossary.md` - verify entries for **Aggregated review flag**, **In-asset interest**, and the refined **Zero-basis review** entry already exist and match the plan's definitions; update if wording diverged (these were added in the r1 commit; verify rather than re-add). Drop the **Reward dust** entry at `docs/maintenance/glossary.md:25` (confirmed present - the entry text begins with `Reward dust - Reward-income row...`; added during the r1 fold). The entry is no longer used after the r9 Task-2 removal; the concept is preserved in `docs/history/feature-notes/2026-07-15-review-flag-deferred-findings.md` §7 only.
- [x] `docs/maintenance/crypto_reporting_guidelines.md` - add a CRG rule covering the aggregation-boundary re-evaluation principle (zero-basis reasons dropped from aggregated rows when material; per-lot signals preserved)
- [x] `docs/maintenance/crypto_implementation_guidelines.md:591` (Missing Path Coverage bullet) - update from the two-status `"Open loan" / "Overpaid"` framing to enumerate the FIVE loan-status sentinels (`LOAN_STATUS_SETTLED`, `LOAN_STATUS_OPEN_LOAN`, `LOAN_STATUS_IN_ASSET_INTEREST`, `LOAN_STATUS_NO_EUR_PRICE`, `LOAN_STATUS_OVERPAID_VERIFY`); note that branch-(b) (repayment-only, `received_amount == 0`) and branch-(d) (large-overshoot, `> LOAN_OVERSHOOT_INTEREST_PCT`) BOTH route to `LOAN_STATUS_OVERPAID_VERIFY` (no sixth sentinel). Task 2 rewrites the classifier this bullet describes.
- [x] `docs/maintenance/crypto_implementation_guidelines.md:413-419` (`### Aggregation of review_reason`) - keep the existing join explanation (still used as the pre-filter input) and add that the aggregated entry's `review_required`/`review_reason` are then re-derived via `_re_evaluate_aggregated_review` (cite the new CRG rule); the joined-reason reduction is no longer the final word on the aggregated row's review fields.
- [x] `docs/maintenance/koinly_guidelines.md` - note the loan-repayment amount-space overshoot classification, the five loan-status sentinels (`Settled` / `Open loan` / `Likely in-asset interest` / `Cannot classify: no EUR price data` / `Overpaid (verify)`), and the `LOAN_OVERSHOOT_INTEREST_PCT = Decimal("1")` threshold
- [x] `docs/maintenance/tax_reporting_guidelines.md:53-56` (Loan Activity Section) - update unconditionally (the hedge "only if Excel-section text is documented there" is stale; the doc DOES contain "cross-year loan" at line 55, which the Validation Commands grep sweep would otherwise flag as a survivor). Describe the five loan-status sentinels (`Settled` / `Open loan` / `Likely in-asset interest` / `Cannot classify: no EUR price data` / `Overpaid (verify)`) and their fills (neutral / neutral / neutral / yellow / light-red).
- [x] `README.md:179` - update unconditionally (the hedge is stale; the line currently reads "overpaid balances (cross-year loan repayments) are highlighted with red fill for manual review"). Replace with the five-sentinel classification summary; also fix "red fill" → "light-red fill" to match the rest of the codebase.
- [x] Commit: `docs(crypto): capture review-flag aggregation-boundary rule and loan classification`

### Task 4 - Optional ADR (skip if not all three criteria)

Files:
- `docs/maintenance/project-decisions.md`

- [x] Evaluate whether the aggregation-boundary re-evaluation rule meets all three ADR criteria (hard to reverse, surprising without context, real trade-off). If yes, append `## PD-008: Review-flag re-evaluation at the aggregation boundary` to `docs/maintenance/project-decisions.md` (the file uses `## PD-NNN:` format; PD-001 through PD-007 already exist, verified at lines 5/9/13/17/21/25/29). If no, skip; the CRG rule in Task 3 suffices. SKIPPED: rule fails criterion #1 (trivially reversible, one helper + one call site) and is weak on #3 (presentation/severity choice, not architectural). CRG-020 (Task 3) captures the principle at the right granularity; the rule is a localized refinement within PD-005's materiality/review-gating framework, not a new top-level decision. See task-4-implement.log.md for the full evaluation.

## Monitor

- **Reward-FMV-not-carried-into-basis detection.** Owner: deferred-findings note at `docs/history/feature-notes/2026-07-15-review-flag-deferred-findings.md`. After Task 1 lands, re-inspect the user's 2025 export for any aggregated disposal row that still shows a zero-basis reason *because every lot in the group is zero-basis*. If such rows exist and correlate with reward-origin lots, that is evidence Koinly did not carry reward FMV into basis; open a dedicated plan.
- **FEE-token-lot filtering at the lot parser.** Owner: same note. 99 Koinly-tracking rows in the 2025 CG export pass through `_parse_capital_gains_file` and reach the per-lot review list. After Task 1 they no longer poison aggregated rows, but they still add noise to `context.review_entries`. Decide later whether to filter them at parse time.
- **`LoanBalanceStatus` enum promotion.** Owner: deferred-findings note. r7 architecture finding #10: `balance_status` remains typed as `str` despite the closed five-element sentinel set; the type system cannot enforce the sentinel boundary that Invariant 6 polices by convention. The project already has the sealed-class pattern (e.g. `DerivativesClassification` in `application/crypto/entities.py`). When the codebase next touches `LoanActivityEntry` for an unrelated change, consider promoting `balance_status` to a `LoanBalanceStatus` enum and updating the fill mapping to match on enum members; add mypy enforcement so a future fixture passing a literal string fails at type check.
- **`_NO_EUR_PRICE_FILL` / `_YELLOW_FILL` duplication.** Owner: deferred-findings note. r8 simplification+architecture finding #9: Task 2 mandates a new module-level `_NO_EUR_PRICE_FILL = PatternFill(start_color="FFFFFF00", ...)` in `loan_activity_sheet.py` that is byte-identical to the existing `_YELLOW_FILL` at `crypto_gains_sheet.py:21`. The r7 #13 fold institutionalised the duplication by mandating both fills side-by-side. When a shared fills module is next convenient (e.g. adding a third yellow consumer), hoist `_YELLOW_FILL` to `excel_utils.py` (or a new `persisting/_fills.py`) and have both sheets import it; until then the two byte-identical literals stay in their respective modules.
- **`_ZERO_BASIS_REASON_PREFIXES` re-encodes upstream constants.** Owner: deferred-findings note. r8 simplification+architecture finding #10: Task 1's prefix tuple re-encodes literal strings already defined as module-level constants in `fifo_helpers.py:25-34` (`_ZERO_COST_REASON`, `_ZERO_PROCEEDS_REASON`, `_ZERO_COST_NEGATIVE_PROCEEDS_REASON`). An upstream reword must touch both sites. The prefix tuple intentionally uses shorter prefixes (`"Zero acquisition cost"`, `"Zero disposal proceeds"`) that subsume multiple upstream constants via `startswith`, so the replacement is not a 1:1 import - it requires either broadening the upstream constants or keeping two of the three prefix entries as literals. When the codebase next touches either site, decide the right merge shape and replace the re-encoded literals accordingly.
- **Three copy-pasted sheet-test methods collapse to parametrize.** Owner: deferred-findings note. r8 simplification finding #11: `TestLoanActivitySheetSettled`, `TestLoanActivitySheetOpenLoan`, and the `TestLoanActivitySheetInAssetInterest` no-fill assertion all share an identical fixture shape (only the sentinel differs: `LOAN_STATUS_SETTLED`, `LOAN_STATUS_OPEN_LOAN`, `LOAN_STATUS_IN_ASSET_INTEREST`). Could be one `@parametrize` over three no-fill sentinels. The plan prescribes three explicitly-named methods because they appear in the r6/r7 fold log by name and the method names are load-bearing for grep; when the test file is next refactored, collapse the three into a single parametrized test and update the fold-log references.

## Plan review

- r1 pre-revision (2026-07-15): `docs/history/reviews/2026-07-15-plan-review-review-flag-aggregation-boundary-r1-pre-revision.md` - Blocker=3, Medium=3, Low=4, Monitor=1; ready=No. Folded: B1 (RED skeleton-first), B2 (Invariant 2 atomic fields), B3 (4th loan bucket + Invariant 4 fix), M4 (assert via constants), M5 (drop `known_assets`), M6 (Section 4 split), L7 (strip-set assertion), L8 (`LOAN_OVERSHOOT_INTEREST_PCT` in `loan_activity.py`), L9 (sentinel + `balance_detail` field), L10 (add pitfalls doc + koinly doc), M11 (extend grep).
- r1 post-revision (2026-07-15): `docs/history/reviews/2026-07-15-plan-review-review-flag-aggregation-boundary-r1.md` - Blocker=1, Medium=1, Low=5, Monitor=0; ready=No. Prior 6 findings all verified Fixed. New findings folded: B1 (add `test_extract_loan_activity_overpaid_status` at `test_crypto_reporting.py:6370-6383` and `TestLoanActivitySheetPerAssetBalance` at `test_loan_activity_sheet.py:87-133` to Task 3 rewrite scope), M2 (delete `LOAN_STATUS_OVERPAID` outright - grep confirmed zero external callers), L5 (Invariant 5 precedence: branch (b) wins over (a) when both `received_amount == 0` and `received_value_eur == 0`), L7 (alias for `LOAN_STATUS_OVERPAID` already covered by M2's outright delete).
- r2: `docs/history/reviews/2026-07-15-plan-review-review-flag-aggregation-boundary-r2.md` - Blocker=0, Medium=0, Low=5, Monitor=0; ready=Yes. All 5 Lows folded: (1) "17 cases" → "21 cases" in Evaluation Criteria; (2) Task 3 now mandates replacing inline `"Settled"`/`"Open loan"` literals at `loan_activity.py:110,114` with `LOAN_STATUS_SETTLED`/`LOAN_STATUS_OPEN_LOAN`; (3) `TestLoanActivitySheetPerAssetBalance` WBTC fixture already satisfies branch (b) - leave numbers as-is, change only the `balance_status` argument; (4) `test_extract_loan_activity_overpaid_status` fixture is already a 100% overshoot - change only the assertion, rename the test; (5) noted that `_make_loan_activity_entry` helper needs no edit.
- r3: `docs/history/reviews/2026-07-15-plan-review-review-flag-aggregation-boundary-r3.md` - Blocker=0, Medium=11, Low=8, Monitor=0; ready=No. All 11 Mediums + 8 Lows folded in this revision: (1) circular import - inline filter into `aggregation.py`, drop the new `review_reason_filter.py` module; (2) strip-set prefix-match replaces full-string equality, drops the regression-guard test; (3) classification tests moved to `test_crypto_reporting.py#TestExtractLoanActivityClassification` calling `_extract_loan_activity` via TH CSV fixtures; (4) added `test_strips_at_exact_materiality_boundary` + renamed below-threshold test to use `Decimal("0.99")`; (5) added parametrized `test_strips_each_zero_basis_prefix_individually`; (6) added `test_mixed_dust_and_valued_rows_in_same_group_split_correctly`; (7) added `detail + dust == len(taxable_now_entries)` assertion to reconciliation test; (8) reworded Section 4 test to specify SIX rows (four unchanged + two new); (9) `tax_reporting_guidelines.md:53-56` update made unconditional; (10) stale-string sweep prose corrected (exact old label must be zero-hit; `cross-year loan` substring is the one that legitimately still matches the new sentinel); (11) Task 1 inlined filter (no new module). Lows: (12) `ADR-NNN` → `PD-008` in Task 5; (13) README.md:179 update made unconditional + `red` → `light-red`; (14) `_parse_income_file` removed from Review Scope must-fix; (15) r2-finding-3 "branch-(b) coverage" rationale corrected (sheet test doesn't invoke the classifier); (16) `balance_detail` rendered in sibling column 10, fill loop extended to `range(1, 11)`; (17) `crypto_implementation_guidelines.md:591` and `:413-419` updates added to Task 4; (18) Invariant 6 rationale corrected (`classification-layer`, not `presentation-layer`); (19) "no new public API surface" claim softened to name the one new symbol (`LOAN_OVERSHOOT_INTEREST_PCT`).
- r4: `docs/history/reviews/2026-07-16-plan-review-review-flag-aggregation-boundary-r4.md` - Blocker=0, High=1, Medium=3, Low=9, Monitor=0; ready=No. 9 of 13 findings folded in this revision (4 simplification Lows noted but not applied): (1, High) `_re_evaluate_aggregated_review` crashes on `review_reason is None`  -  added None-guard as first line of helper body (Invariant 2 extended) + dedicated `test_none_review_reason_with_material_values_returns_unchanged`; (2, Medium) `cost_eur > 0` and `proceeds_eur > 0` clauses individually unpinned  -  added `test_preserves_zero_basis_when_only_cost_is_zero` and `test_preserves_zero_basis_when_only_proceeds_are_zero`; (3, Medium) existing `test_reconciliation_key_value_pairs` at `:549-575` would KeyError on the deleted reconciliation key  -  added explicit Task 2 sub-step to re-scope it (and sibling `test_reconciliation_empty_rewards`); (4, Medium) "four new constants" contradicted the five Task-3 adds  -  fixed Review Scope bullet to "all five new"; (5, Low) `"overshoot 0.58%"` arithmetic wrong  -  corrected to `"0.5853%"` (4dp ROUND_HALF_UP); (6, Low) ruff check omitted the three modified test files  -  extended Validation Commands; (7, Low) `test_strips_each_zero_basis_prefix_individually` missed the `"Zero EUR value for known crypto asset"` prefix  -  added a fourth synthetic parametrize case; (8, Low) header line 3 omitted the 8 Lows from r3 fold  -  corrected to "11 Medium + 8 Low"; (9, Low) "four status buckets" enumerated five in the same parenthetical  -  fixed to "five loan-status sentinels". Simplification Lows 10-13 noted but not applied: (10) `_ZERO_BASIS_REASON_PREFIXES` module-level constant retained (readability over a literal-in-body; the `split(":")[0]` alternative was rejected because not all upstream reasons use the colon separator); (11) sibling column 10 retained (Invariant 7 design decision); (12) `TestExtractLoanActivityClassification` kept as six methods rather than one parametrized function (each branch has a distinct fixture shape); (13) Task 4 doc updates confirmed load-bearing (no action).
- r5: `docs/history/reviews/2026-07-16-plan-review-review-flag-aggregation-boundary-r5.md` - Blocker=0, High=0, Medium=1, Low=12, Monitor=0; ready=No. All 13 findings folded in this revision: (1, Medium) yellow fill for `LOAN_STATUS_NO_EUR_PRICE` had no sheet test - added new `TestLoanActivitySheetNoEurPrice` test class (positive yellow-fill assertion + negative IN_ASSET_INTEREST no-fill assertion) and pinned the yellow hex code `FFFFFF00` in the implementation step; (2, Low) `range(1, 11)` fill-loop extension unpinned - mandated the bump in `TestLoanActivitySheetOverpaid`, `TestLoanActivitySheetSettled`, and the new `TestLoanActivitySheetInAssetInterest`; added `TestLoanActivitySheetSettled` to Task 3 scope; (3, Low) multi-group dust summary unpinned - added `test_multiple_asset_wallet_groups_each_emit_their_own_summary_line`; (4, Low) multi-survivor partial-strip unpinned - extended `test_joined_reason_partial_strip` to a 3-part/2-survivor parametrize case; (5, Low) `loan_activity_sheet.py:18-30,37-40` docstring and inline comment stale - added explicit Task 3 sub-step; (6, Low) `ROUND_HALF_UP` rounding mode not specified - added explicit `quantize(Decimal("0.0001"), rounding=ROUND_HALF_UP)` in the implementation step; (7, Low) line 220 "describe the four statuses" - fixed to "five loan-status sentinels"; (8, Low) line 241 "four status buckets" - fixed to "five loan-status sentinels"; (9, Low) line 70 "four buckets" terminology clash - reworded to "four fill behaviors across five sentinel constants"; (10, Low) 12 em dashes (U+2014) violated the hard rule - replaced with ASCII ` - `; (11, Low) fold-log item (12) said "five methods" but Task 3 prescribes six - corrected to "six methods"; (12, Low) fold-log opening "All 13 findings folded" contradicted its own tail "10-13 noted but not applied" - corrected to "9 of 13 findings folded"; (13, Low) `test_received_zero_and_no_eur_price_routes_to_overpaid_verify_not_cannot_classify` duplicated `test_repayment_only_asset_routes_to_overpaid_verify` - dropped the standalone test and folded its EUR-price qualifier into the repayment-only test as parametrize case (ii).
- r6: `docs/history/reviews/2026-07-16-plan-review-review-flag-aggregation-boundary-r6.md` - Blocker=0, High=0, Medium=2, Low=6, Monitor=0; ready=No. All 8 findings folded in this revision: (1, Medium) column-10 `balance_detail` value rendering unpinned by sheet tests (r5 fold-verification gap - the range bumps landed but the value assertion was dropped) - mandated `ws.cell(data_row, 10).value == entry.balance_detail` in `TestLoanActivitySheetInAssetInterest` (overshoot case) AND `ws.cell(data_row, 10).value is None` in `TestLoanActivitySheetNoEurPrice` and `TestLoanActivitySheetSettled`; (2, Medium) misleading "No taxable-now rewards" empty-label when all taxable-now rows are dust - added explicit empty-label conditional to Task 2 implementation step: write "All taxable-now rows classified as dust - see summary below" when `real_rows` is empty AND `dust_rows` is non-empty, "No taxable-now rewards" only when both are empty; (3, Low) fold-log "seven methods" should be "six methods" (Task 3 prescribes six `TestExtractLoanActivityClassification` methods, not seven) - corrected in both r4 and r5 fold-log entries; (4, Low) missing import sub-step for popular-token helpers - added explicit `from ..crypto.classification import _contains_popular_token, _get_popular_crypto_tokens` to Task 2 implementation step; (5, Low) dust summary literal format not echoed - added exact f-string template `f"{asset} dust ({wallet}): {count} rows, summed Value EUR = {summed:.2f} (Koinly 2-decimal export). Re-export with higher precision to verify."` to Task 2 implementation step; (6, Low) `ROUND_HALF_UP` unpinned by discriminating test - extended `test_small_overshoot_with_eur_classified_as_in_asset_interest` to TWO parametrize cases: (i) production-shape `0.06151/0.06187` (non-discriminating; pins fixture), (ii) discriminating `received=Decimal("10000"), repaid=Decimal("10058.525")` → raw 0.58525%; ROUND_HALF_UP at 4dp gives 0.5853, ROUND_HALF_EVEN gives 0.5852 (predecessor digit 2 is even); (7, Low) `TestLoanActivitySheetOpenLoan` no-fill assertion missing - added `TestLoanActivitySheetOpenLoan` (existing class at `:160-182`) to Task 3 rewrite scope with no-fill assertion across `range(1, 11)` and column-10 None assertion; (8, Low) duplicate IN_ASSET_INTEREST no-fill test in `TestLoanActivitySheetNoEurPrice` - dropped the `test_in_asset_interest_has_no_fill` clause from line 223; `TestLoanActivitySheetInAssetInterest` is the sole host for that assertion.
- r7: `docs/history/reviews/2026-07-16-plan-review-review-flag-aggregation-boundary-r7.md` - Blocker=0, High=1, Medium=8, Low=8, Monitor=0; ready=No. 15 of 17 findings folded; 2 architecture Mediums deferred to Monitor (design preferences; folding would expand scope and risk further divergence): (1, High, security) CSV injection - Task 2 dust-summary f-string interpolated CSV-derived `asset`/`wallet` without `safe_cell_value()`; mandated wrapping both interpolations via the module's existing sanitizer (lines 65/72/75/76/82); (2, Medium, architecture - DEFERRED to Monitor) Task 2 layering violation (sheet writer as classification site); (3, Medium) `TestLoanActivitySheetOpenLoan` stale method name `test_loan_activity_open_loan_no_highlight` corrected to the actual `test_loan_activity_flags_open_loan` (existing method at `:163`); (4, Medium) `TestLoanActivitySheetOverpaid` column-10 assertion was tautological (`None == None`) because the fixture omitted `balance_detail`; mandated `balance_detail="overshoot 5.0000%"` on the fixture so the assertion actually pins the column-10 write; (5, Medium) empty-label branch evaluation site ambiguous - specified "evaluate at call site in `write_crypto_supplementary_sheet`; do NOT change the `_write_reward_detail_rows` helper signature; pass the chosen `empty_label` to the existing helper"; (6, Medium) empty-label conditional had no discriminating test - extended `test_popular_asset_zero_value_rows_render_as_summary` to assert the Section 2 empty-label cell equals exactly `"All taxable-now rows classified as dust - see summary below"`; (7, Low) lines 222+224 overlap on `TestLoanActivitySheetOverpaid` range bump - dropped the bump directive from line 222, leaving line 224 as the sole host for bump + column-10 value assertion; (8, Medium) WBTC overshoot 0.30% (Problem line 23 / Examples line 59) contradicted 0.5853% (Task 3 fixture line 209) - updated Problem and Examples to read "0.59%" / `"overshoot 0.5853%"` matching the fixture; (9, Medium) Task 4 line 245 directed "enumerate the four loan-status sentinels plus the repayment-only branch-(b) case" - reworded to enumerate the FIVE sentinels explicitly and note that branches (b) and (d) BOTH route to `LOAN_STATUS_OVERPAID_VERIFY`; (10, Medium, architecture - DEFERRED to Monitor) `LoanBalanceStatus` enum promotion; (11, Low) `TestLoanActivitySheetInAssetInterest` announcement duplicated across lines 222+225 - dropped the generic announcement from line 222, leaving line 225 as the sole host; (12, Low) `balance_detail` cell-write without `safe_cell_value` - mandated wrapping via the module's existing sanitizer; (13, Low) `_NO_EUR_PRICE_FILL` module-level promotion without symmetric treatment of `red_fill` - mandated promoting both fills to module-level constants side-by-side (`_LIGHT_RED_FILL` + `_NO_EUR_PRICE_FILL`); (14, Low) ROUND_HALF_UP impl-step pin redundant given r6 #6 discriminating test - retained as defense-in-depth (no change; the test is the primary enforcer but the impl pin documents intent); (15, Low) "three fill behaviors" (line 220) vs "four fill behaviors" (line 70) terminology drift - unified wording at line 220 to "three fill colors applied across four behavior branches"; (16, Low) "four-bucket" terminology in Task 3 header (line 197), Review Scope (line 95), commit message (line 231), and README (line 249) - reworded all four to "five-sentinel classification (four classifier branches)"; (17, Low) `TestLoanActivitySheetSettled` line citation `(line 225-...)` pointed mid-class - corrected to `(class at :199-228, method at :203)`.
- r8: `docs/history/reviews/2026-07-16-plan-review-review-flag-aggregation-boundary-r8.md` - Blocker=0, High=0, Medium=8, Low=4, Monitor=3 new (carrying 2 from r7 = 5 total Monitor); ready=No. 9 of 12 findings folded; 3 architectural Mediums deferred to Monitor (same family as r7's deferred #2/#10; folding would re-escalate the architecture loop): (1, Medium) Review Scope line 95 still read "covers the four-bucket classifier" despite r7 #16 fold-log claiming "reworded all four sites" - reworded line 95 to "covers the five-sentinel classifier - four Invariant-5 branches plus the unchanged Settled/Open-loan branches" (r7 fold-verification gap); (2, Medium) Invariant 7 line 115 still used `"overshoot 0.30%"` example despite r7 #8 correcting lines 23/59 to 0.59%/0.5853% - updated line 115 to `"overshoot 0.5853%"` (r7 fold-verification gap); (3, Medium) Task 3 implementation step prescribed `raw_pct.quantize(...)` without carving out branch (b) where `raw_pct is None` (because `received_amount == 0`) - added explicit carve-out: "Branch (b) sets `balance_detail = "received_amount=0; repayment-only asset"` directly; do NOT call `.quantize(...)` on `raw_pct` for this branch"; (4, Medium) Section 4 reconciliation at line 192 references `real_rows`/`dust_rows` but the plan did not state they are computed function-local in `write_crypto_supplementary_sheet` - mandated function-local scope so both Section 2 and Section 4 share the same partition; (5, Medium) Task 2 implementation step did not specify summary-block positioning/gating (when to write the `"Dust summary:"` header, when to skip it, position relative to the empty-label row) - mandated: append summary block with bold `"Dust summary:"` header at `row_no` when `dust_rows` is non-empty, sorted by `(asset, wallet)`; skip entirely when `dust_rows` is empty; (6, Medium) `test_niche_asset_zero_value_keeps_per_row_yes_flag` did not tell the implementer to set `review_required=True, review_reason="Zero EUR value for known crypto asset - test fixture"` on the OSBGT fixture - the default `_make_reward_entry` has `review_required=False` which renders as `NO` and makes the assertion vacuous; mandated the fixture fields and the explicit cell-read assertion; (7, Medium) `test_overshoot_boundary_1pct_inclusive` parametrize omitted `received_value_eur > 0` - branch (a) `received_value_eur == 0 → LOAN_STATUS_NO_EUR_PRICE` fires BEFORE branch (c), so a fixture with `Net Value (EUR)=0` routes all three boundary cases to `NO_EUR_PRICE` and the test goes RED for the wrong reason; mandated `received_value_eur > 0` on all three parametrize cases; (8, Medium) Task 4 updated `crypto_implementation_guidelines.md:591` and `:413-419` but missed the `:1605-1610` paragraph stating zero-value popular-token rewards are FLAGGED for review - added explicit Task 4 directive to update `:1605-1610` to describe the dust-summary behavior; (9, Medium, architecture - DEFERRED to Monitor) `_NO_EUR_PRICE_FILL` byte-identical to `crypto_gains_sheet.py:21`'s `_YELLOW_FILL` (r7 #13 fold institutionalised the duplication); (10, Medium, architecture - DEFERRED to Monitor) `_ZERO_BASIS_REASON_PREFIXES` re-encodes strings already in `fifo_helpers.py:25-34`; (11, Medium, simplification - DEFERRED to Monitor) three copy-pasted sheet-test methods (`TestLoanActivitySheetSettled`/`OpenLoan`/`InAssetInterest` no-fill assertions) could collapse into one parametrized test over three no-fill sentinels; (12, Low) Task 3 implementation step did not explicitly say to update the import at `loan_activity.py:10` to bring in all five sentinels and drop the `LOAN_STATUS_OVERPAID` import - added explicit import-update directive.
- r9: `docs/history/reviews/2026-07-16-plan-review-review-flag-aggregation-boundary-r9.md` - Blocker=0, High=1, Medium=5, Low=17, Monitor=0 new (4 carried from r7/r8 deferrals + 2 pre-existing originals = 6 total Monitor entries; 1 r7 entry removed as moot after Task 2 deletion); ready=No. **Task 2 (Crypto Supplementary dust summary) REMOVED wholesale** at user direction after the panel surfaced a design-level defect (HIGH #1): the discriminator `r.asset in _get_popular_crypto_tokens() or _contains_popular_token(r.asset)` was treated as equivalent to "Koinly can price this asset", but the popular-token set is an explicit don't-ignore allowlist that SURFACES zero-value rewards for manual review (per `docs/maintenance/tax/popular_crypto_tokens.json` description and the user's confirmed intent for OSBGT/PBERA/SWBERA/STBGT/BGT). The original Task 2 design is preserved verbatim with a proposed alternative discriminator (has-any-priced-row) in `docs/history/feature-notes/2026-07-15-review-flag-deferred-findings.md` §7 for a future focused plan. Removal also folded: r9 #5/17 Lows against Task 2 specifics (OSBGT fixture, dust literal template, multi-group partition tests, Section 4 reconciliation split, `:1605-1610` doc update, reward-dust glossary entry, popular-token import sub-step) - all became moot. Findings against the remaining Tasks 1 and 3 folded: (#2, Medium) line 95 "four-bucket classifier" surviving r7 #16 + r8 #1 fold claims - finally fixed by rewording to "five-sentinel classifier - four Invariant-4 branches plus the unchanged Settled/Open-loan branches" (fold-verification gap x2, r7 #16 and r8 #1 both claimed the fix landed but the Edit hit line 204 instead); (#3, Medium) r8 fold-log falsely claimed line 95 fix was applied - corrected in this r9 fold-log entry (the r8 entry remains as historical record); (#4, Medium) three missing import directives - added explicit steps at `loan_activity_sheet.py:13` (production), `test_loan_activity_sheet.py` (collection-time failure otherwise), and `test_crypto_reporting.py:12-19` (`LOAN_OVERSHOOT_INTEREST_PCT` + five sentinels); (#5, Low) `_LIGHT_RED_FILL` hex placeholder `"...light-red..."` - replaced with the concrete `FFFFCCCC` (the existing `loan_activity_sheet.py:62` literal; do NOT change the color); (#6, Low) line 70 (Evaluation Criteria) "red" → "light-red" terminology drift - fixed. Additional Lows folded: Invariant renumbering (drop old 4 and 8, leaving 1/2/3/5/6/7 → renumbered 1-6); Task renumbering (drop Task 2, so old Task 3/4/5 → new Task 2/3/4); Validation Commands and release gates trimmed to drop `crypto_supplementary_sheet.py` and `test_crypto_supplementary_sheet.py`; Problem statement shrunk from three flags to two with a footnote explaining the removal; Evaluation Criteria "reward dust vs unpriced" dimension dropped; Review Scope dropped `crypto_supplementary_sheet.py` and `test_crypto_supplementary_sheet.py` from must-fix; "Task 2 layering" Monitor entry removed (applies to the deferred feature, not this plan); the `_NO_EUR_PRICE_FILL` Monitor entry's "Task 3" reference corrected to "Task 2" (the new Task 2 = Loan Activity). The 17 Lows also include various spec-precision nits against Task 2 specifics that became moot on removal; they are not enumerated individually here. Counter reset to 0 clean rounds (substantive revision per plans skill #9).
- r10: `docs/history/reviews/2026-07-17-plan-review-review-flag-aggregation-boundary-r10.md` - Blocker=0, High=2, Medium=2, Low=18 (13 selected for fold: L1-L10 + L15-L17; 5 deferred: L11-L14 verbosity trims + L18 historical-convention no-fold); Monitor=0 new (6 carried); ready=No. 4 load-bearing findings folded: (H1) `test_repayment_only_asset_routes_to_overpaid_verify` parametrize case (i) prescribed an impossible fixture shape (`received_amount == 0 AND received_value_eur > 0` - the loan-receipt branch increments both fields jointly in `_extract_loan_activity`); case (i) dropped, test reduced to the single reachable shape; (H2) `test_strips_each_zero_basis_prefix_individually` references `fifo_helpers` constants but no import directive existed - added explicit import directive for `_ZERO_COST_REASON` and `_ZERO_PROCEEDS_REASON`; (M1) `test_no_eur_price_classified_as_cannot_classify` said "non-zero amounts" without specifying which - made explicit (`received_amount > 0 AND repaid_amount > 0 AND received_value_eur == 0 AND repaid_value_eur == 0`); (M2) new test classes (`TestReEvaluateAggregatedReview`, `TestExtractLoanActivityClassification`, `TestLoanActivitySheetNoEurPrice`, `TestLoanActivitySheetInAssetInterest`) had no directive to add `@pytest.mark.unit` - added explicit decoration directives so the `-m unit` release gate collects them. Selected Lows folded: (L1) Monitor entry named `PHANTOM_LOT_REASON` (does not exist in `fifo_helpers.py`) - corrected to the actual three constants and the prefix-subsumption rationale; (L2) `_ZERO_COST_NEGATIVE_PROCEEDS_REASON` parametrize case redundant (shares the `"Zero acquisition cost"` prefix with `_ZERO_COST_REASON`) - dropped, leaving THREE parametrize cases; (L3) Task 2 Files list mentioned 2 of 6 sheet-test classes - extended to enumerate all six; (L4) `TestLoanActivitySheetSettled` line citation `:199-228` off-by-one (decorator at :199, class at :200) - corrected; (L5) `aggregation.py:322-336` excluded the closing paren at :337 - corrected to inclusive `:322-337`; (L6) glossary cleanup directive was conditional ("if it was added") - made unconditional with verified line citation `:25`; (L7) `_LIGHT_RED_FILL` promotion directive did not direct deletion of the dead function-scoped `red_fill` at `loan_activity_sheet.py:62` - added cleanup directive; (L8) `test_large_overshoot_routes_to_overpaid_verify` gave amount values without the TH CSV column layout - added pointer to the existing fixture at line 6286; (L9) Invariant 4's `loan_activity.py:99-103` citation ambiguous (in elif branch) - refined to cite the branch header at `:71`; (L10) Examples parenthetical "no receipt rows" conflated row-shape with accumulator state - dropped "no receipt rows", kept `received_amount == 0`; (L15) header "Tasks 1/3" stale after r9 renumbering - corrected to "old Tasks 1 and 3 (now Tasks 1 and 2)"; (L16) Monitor qualifier "on a popular asset" stale after r9 fold - dropped; (L17) r9 fold-log Monitor count prose refined to "4 carried from r7/r8 + 2 pre-existing = 6 total" (prior phrasing "5 carried from r7/r8" was arithmetically inconsistent). Counter remains at 0 clean rounds (substantive revision per plans skill #9).
- r11: `docs/history/reviews/2026-07-17-plan-review-review-flag-aggregation-boundary-r11.md` - Blocker=0, High=0, Medium=6, Low=8 (selected Lows folded: L1, L3, L4, L5, L7, L8; deferred: L2 merged into Me3, L6/L9 optional-convention); Monitor=0 new (6 carried); ready=No. 6 Medium fold-completeness gaps folded: (Me1) `TestLoanActivitySheetOpenLoan#test_loan_activity_flags_open_loan` directive used verb "bump" implying an existing fill-check loop - the existing test has none; rewritten to "ADD a new no-fill assertion loop `for col in range(1, 11): assert not _is_light_red_fill(...) and not _is_yellow_fill(...)` AND assert `ws.cell(data_row, 10).value is None`"; (Me2) Review Scope parallel bullet at line 84 still enumerated only 2 of 6 sheet-test classes (r10 L3 fold landed only on Task 2 Files list, missed the parallel Review Scope site) - extended to enumerate all six classes with Task 2 cross-reference; (Me3) r10 H2 fold chose the over-specified alternative (new standalone bullet) re-introducing the L13-deferred pattern - consolidated lines 146 and 172 into a single Task-2-level import directive covering seven symbols (`_ZERO_COST_REASON`, `_ZERO_PROCEEDS_REASON`, `LOAN_OVERSHOOT_INTEREST_PCT`, five sentinels) from three modules (`fifo_helpers`, `loan_activity`, `domain.constants`); (Me4) M2 `:199` citation contradicted the L4 correction in the same revision - clarified as "decorator lines (`:86/136/159/199`); class declarations at `:87/137/160/200`"; (Me5) r10 fold-log header "Low=14" arithmetically wrong - corrected to "Low=18 (13 selected: L1-L10 + L15-L17; 5 deferred: L11-L14 verbosity trims + L18 historical-convention no-fold)"; (Me6) r9 fold-log Monitor count prose "5 carried from r7/r8, for 6 total" was arithmetically inconsistent - refined to "4 carried from r7/r8 deferrals + 2 pre-existing originals = 6 total Monitor entries; 1 r7 entry removed as moot after Task 2 deletion". Selected Lows folded: (L1) Monitor entry `fifo_helpers.py:25-31` truncated mid-constant - corrected to `:25-34`; (L3+L4) historical-rationale parentheticals (H1 case-(i) drop rationale; L2 redundancy rationale) dropped from plan body; (L5) r10 attribution trailers (`(r10 finding MN)`) stripped from executable body; (L7+L8) Monitor entry historical-rationale parentheticals (L16/L1 corrections) dropped. Plan shrank from 245 to 244 lines. Counter remains at 0 clean rounds (substantive revision per plans skill #9).
- r12: `docs/history/reviews/2026-07-17-plan-review-review-flag-aggregation-boundary-r12.md` - Blocker=0, High=0, Medium=1, Low=6 (1 Medium folded: missing r11 fold-log entry; 6 simplification Lows deferred as same family as r10 L11-L14 verbosity trims); Monitor=0 new (6 carried); ready=Yes. The executable plan body has been stable through two consecutive rounds (r11 + r12) on quality/testing/architecture/security/consistency. The sole r12 Medium was doc-completeness: the r11 fold-log entry (above) was not appended to the plan body in the prior turn, so an implementer reading the plan in isolation could not see what r11 changed without leaving the plan for the r11 staging doc. Folded by appending the r11 entry and prepending r11 to header line 3. The six simplification Lows are historical-rationale parentheticals and verbosity trims in the same family as r10 L11-L14 (consistently deferred). Plan declared ready: no executable-content defects remain; subsequent implementation work can proceed via the `execute-plan` skill.
